<local:CWindow x:Class="ModCreator.Windows.ProjectEditorWindow" x:TypeArguments="localData:ProjectEditorWindowData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:ModCreator.Windows"
        xmlns:localData="clr-namespace:ModCreator.WindowData" xmlns:helper="clr-namespace:ModCreator.Helpers"
        xmlns:converter="clr-namespace:ModCreator.Converters" xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit" mc:Ignorable="d"
        Title="{x:Static helper:ProjectEditorWindowText.ProjectEditorTitle}" Height="1080" Width="1920" WindowStartupLocation="CenterScreen"
        WindowState="Maximized" Icon="/Resources/app-icon.png" FontFamily="Segoe UI" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <converter:BoolToIconConverter x:Key="BoolToIconConverter"/>
        <converter:FilePathToImageConverter x:Key="FilePathToImageConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FF2E5090" Padding="15" Margin="0" CornerRadius="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBox Text="{Binding Project.ProjectName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="20" FontWeight="Bold" Foreground="White" Background="Transparent" BorderBrush="White" BorderThickness="0,0,0,1" Padding="5,2" CaretBrush="White"/>
                    <TextBlock FontSize="12" Foreground="#FFCCCCCC" Margin="0,5,0,0">
                        <Run Text="{x:Static helper:ProjectEditorWindowText.ProjectIdLabel}"/><Run Text="{Binding Project.ProjectId, Mode=OneWay}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="{x:Static helper:ProjectEditorWindowText.Help}" Click="Help_Click" Style="{StaticResource HeaderButton}" Width="70" Margin="10,0,0,0"/>
                    <Button Content="{x:Static helper:ProjectEditorWindowText.Save}" Click="Save_Click" Style="{StaticResource PrimaryButton}" Width="100" Margin="10,0,0,0"/>
                    <Button Content="{x:Static helper:ProjectEditorWindowText.Cancel}" Click="Close_Click" Style="{StaticResource ActionButton}" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        <TabControl x:Name="tabControl" Grid.Row="1" Margin="10">
            <TabItem Header="{x:Static helper:ProjectEditorWindowText.TabProjectInfo}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="{x:Static helper:ProjectEditorWindowText.ProjectDetailsId}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,0,15,0"/>
                        <TextBox Grid.Column="2" Grid.Row="0" Grid.ColumnSpan="5" Text="{Binding Project.ProjectId}" IsReadOnly="True" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="32" VerticalContentAlignment="Center" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="0" Grid.Row="2" Text="{x:Static helper:ProjectEditorWindowText.ProjectDetailsPath}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,0,15,0"/>
                        <TextBox Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="5" Text="{Binding Project.ProjectPath}" IsReadOnly="True" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="32" VerticalContentAlignment="Center" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="0" Grid.Row="6" Text="{x:Static helper:ProjectEditorWindowText.ProjectDetailsDescription}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,8,15,0"/>
                        <TextBox Grid.Column="2" Grid.Row="6" Grid.ColumnSpan="5" Text="{Binding Project.Description, UpdateSourceTrigger=PropertyChanged}" Height="100" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderBrush="#FFCCCCCC" BorderThickness="1" FontSize="13" Padding="8"/>
                        <TextBlock Grid.Column="0" Grid.Row="4" Text="{x:Static helper:ProjectEditorWindowText.ProjectDetailsAuthor}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,0,15,0"/>
                        <TextBox Grid.Column="2" Grid.Row="4" Grid.ColumnSpan="5" Text="{Binding Project.Author, UpdateSourceTrigger=PropertyChanged}" Height="32" VerticalContentAlignment="Center" BorderBrush="#FFCCCCCC" BorderThickness="1" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="0" Grid.Row="8" Text="{x:Static helper:ProjectEditorWindowText.HeaderCreated}" FontWeight="SemiBold" FontSize="13" Style="{StaticResource LabelText}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <TextBox Grid.Column="2" Grid.Row="8" Text="{Binding Project.CreatedDate, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" IsReadOnly="True" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="32" VerticalContentAlignment="Center" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="4" Grid.Row="8" Text="{x:Static helper:ProjectEditorWindowText.HeaderModified}" FontWeight="SemiBold" FontSize="13" Style="{StaticResource LabelText}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <TextBox Grid.Column="6" Grid.Row="8" Text="{Binding Project.LastModifiedDate, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" IsReadOnly="True" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="32" VerticalContentAlignment="Center" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="0" Grid.Row="10" Text="{x:Static helper:ProjectEditorWindowText.HeaderState}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,0,10,0"/>
                        <TextBox Grid.Column="2" Grid.Row="10" Grid.ColumnSpan="5" Text="{Binding Project.State}" IsReadOnly="True" Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="32" VerticalContentAlignment="Center" FontSize="13" Padding="8,0"/>
                        <TextBlock Grid.Column="0" Grid.Row="12" Text="{x:Static helper:ProjectEditorWindowText.HeaderTitleImg}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource LabelText}" Margin="0,10,10,0"/>
                        <Border x:Name="TitleImageContainer" Grid.Column="1" Grid.Row="12" Grid.ColumnSpan="5" Grid.RowSpan="99" BorderBrush="#FFCCCCCC" BorderThickness="1" Background="#FFF5F5F5" Padding="5" Width="400" Height="300">
                            <Image Source="{Binding Project.TitleImg, Mode=OneWay, Converter={StaticResource FilePathToImageConverter}}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <StackPanel Grid.Row="1" Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{x:Static helper:ProjectEditorWindowText.OpenInExplorer}" Click="OpenProjectFolder_Click" Style="{StaticResource ActionButton}" Width="150" Margin="0,0,5,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.Refresh}" Click="RefreshTab_Click" Style="{StaticResource ActionButton}" Width="100"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="{x:Static helper:ProjectEditorWindowText.TabModConf}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <GroupBox Grid.Column="0" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.ConfFiles}" Margin="0,0,10,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconAdd}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipAddConf}" Click="AddConf_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconClone}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCloneConf}" Click="CloneConf_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconEdit}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipRenameConf}" Click="RenameConf_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconRemove}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipRemoveConf}" Click="RemoveConf_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}"/>
                                <Button x:Name="btnToggleFilterLocalText" Content="{x:Static helper:ProjectEditorWindowText.IconFilter}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipToggleFilterLocalText}" Click="ToggleFilterLocalText_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="10,0,0,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconPattern}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCreateFromPattern}" Click="CreateFromPattern_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="2,0,0,0" Background="#FF4CAF50" Foreground="White"/>
                            </StackPanel>
                            <TreeView Grid.Row="1" Name="tvConfFiles" ItemsSource="{Binding ConfItems, Mode=OneWay}" SelectedItemChanged="TreeView_ConfSelectedItemChanged">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="True"/>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding IsFolder, Converter={StaticResource BoolToIconConverter}}" FontFamily="Segoe UI Symbol" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding Name}" Padding="2"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </GroupBox>
                    <GroupBox Grid.Column="1" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.JsonEditor}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Background="#FFF5F5F5">
                                <Button Content="{x:Static helper:ProjectEditorWindowText.SearchAndReplace}" Click="ReplaceInEditor_Click" Style="{StaticResource ActionButton}" Width="130" Height="28" Margin="0,0,5,0" Background="#FF4CAF50" Foreground="White" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.OpenInNotepad}" Click="OpenInNotepad_Click" Style="{StaticResource ActionButton}" Width="130" Height="28" Margin="0,0,5,0" Background="#FF4CAF50" Foreground="White" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}"/>
                            </StackPanel>
                            <Border x:Name="replacePanel" Grid.Row="1" Background="#FFF9F9F9" BorderBrush="#FFCCCCCC" BorderThickness="0,1,0,1" Padding="10" Visibility="Collapsed">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.Find}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox x:Name="txtFindText" Grid.Row="0" Grid.Column="1" Height="26" VerticalContentAlignment="Center" TextChanged="TxtFindText_TextChanged" PreviewKeyDown="TxtFindText_KeyDown" Margin="0,0,10,5"/>
                                    <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal">
                                        <Button Content="{x:Static helper:ProjectEditorWindowText.FindNext}" Click="FindNext_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Margin="0,0,5,5"/>
                                        <Button Content="{x:Static helper:ProjectEditorWindowText.Close}" Click="CloseReplacePanel_Click" Style="{StaticResource ActionButton}" Width="60" Height="26" Margin="0,0,0,5"/>
                                    </StackPanel>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.Replace}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox x:Name="txtReplaceText" Grid.Row="1" Grid.Column="1" Height="26" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                                    <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal">
                                        <Button Content="{x:Static helper:ProjectEditorWindowText.ReplaceButton}" Click="ReplaceOne_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Margin="0,0,5,0"/>
                                        <Button Content="{x:Static helper:ProjectEditorWindowText.ReplaceAll}" Click="ReplaceAll_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Background="#FF2196F3" Foreground="White"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <avalonedit:TextEditor x:Name="txtJsonEditor" Grid.Row="2" FontFamily="Consolas" FontSize="12" ShowLineNumbers="True" IsEnabled="{Binding HasSelectedConfFile, Mode=OneWay}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                        </Grid>
                    </GroupBox>
                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="{x:Static helper:ProjectEditorWindowText.Translate}" Click="Translate_Click" Style="{StaticResource PrimaryButton}" Width="100" Margin="10,0,0,0"/>
                        <ComboBox x:Name="cmbSourceLanguage" ItemsSource="{Binding SourceLanguages}" SelectedItem="{Binding SelectedSourceLanguage, Mode=TwoWay}" DisplayMemberPath="DisplayName" SelectedValuePath="code" Width="200" Height="32" VerticalContentAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{x:Static helper:ProjectEditorWindowText.NoteDirectFileManagement}" VerticalAlignment="Center" Foreground="Gray" FontSize="11" FontStyle="Italic" Margin="0,0,15,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.OpenInExplorer}" Click="OpenModConfFolder_Click" Style="{StaticResource ActionButton}" Width="150" Margin="0,0,5,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.Refresh}" Click="RefreshTab_Click" Style="{StaticResource ActionButton}" Width="100"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="{x:Static helper:ProjectEditorWindowText.TabModImg}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <GroupBox Grid.Column="0" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.ImageFiles}" Margin="0,0,10,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconFolder}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCreateImgFolder}" Click="CreateImageFolder_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconDelete}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipDeleteImgFolder}" Click="DeleteImageFolder_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding SelectedImageItem.IsFolder, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconImport}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipImportImage}" Click="ImportImage_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconExport}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipExportImage}" Click="ExportImage_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding HasSelectedImageFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconRemove}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipRemoveImage}" Click="RemoveImage_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" IsEnabled="{Binding HasSelectedImageFile, Mode=OneWay}"/>
                            </StackPanel>
                            <TreeView Grid.Row="1" Name="tvImageFiles" ItemsSource="{Binding ImageItems, Mode=OneWay}" SelectedItemChanged="TreeView_ImageSelectedItemChanged">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="True"/>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding IsFolder, Converter={StaticResource BoolToIconConverter}}" FontFamily="Segoe UI Symbol" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding Name}" Padding="2"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </GroupBox>
                    <GroupBox Grid.Column="1" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.ImagePreview}">
                        <Image Source="{Binding SelectedImagePath, Mode=OneWay}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </GroupBox>
                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{x:Static helper:ProjectEditorWindowText.NoteDirectFileManagement}" VerticalAlignment="Center" Foreground="Gray" FontSize="11" FontStyle="Italic" Margin="0,0,15,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.OpenInExplorer}" Click="OpenModImgFolder_Click" Style="{StaticResource ActionButton}" Width="150" Margin="0,0,5,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.Refresh}" Click="RefreshTab_Click" Style="{StaticResource ActionButton}" Width="100"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="{x:Static helper:ProjectEditorWindowText.TabGlobalVariables}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button x:Name="btnGridView" Content="{x:Static helper:ProjectEditorWindowText.GridView}" Click="ToggleGridView_Click" Style="{StaticResource ActionButton}" Background="#FF2E5090" Foreground="White" Width="120" Margin="0,0,5,0"/>
                        <Button x:Name="btnSourceView" Content="{x:Static helper:ProjectEditorWindowText.SourceView}" Click="ToggleSourceView_Click" Style="{StaticResource ActionButton}" Width="120"/>
                    </StackPanel>
                    <DataGrid x:Name="dgGlobalVariables" Grid.Row="1" ItemsSource="{Binding GlobalVariables, Mode=TwoWay}" Style="{StaticResource EditableDataGridStyle}" CellStyle="{StaticResource EditableDataGridCellStyle}" ColumnHeaderStyle="{StaticResource EditableDataGridHeaderStyle}" CanUserAddRows="False" RowEditEnding="DataGrid_RowEditEnding" RowHeight="40" ColumnHeaderHeight="48" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{x:Static helper:ProjectEditorWindowText.VariableName}" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" MinWidth="100"/>
                            <DataGridComboBoxColumn Width="150" MinWidth="100" SelectedValueBinding="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridComboBoxColumn.Header>
                                    <TextBlock Text="{x:Static helper:ProjectEditorWindowText.VariableType}" ToolTip="{x:Static helper:ProjectEditorWindowText.VariableTypeTooltip}"/>
                                </DataGridComboBoxColumn.Header>
                                <DataGridComboBoxColumn.ElementStyle>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="ItemsSource" Value="{Binding DataContext.VarTypes, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                        <Setter Property="DisplayMemberPath" Value="Name"/>
                                        <Setter Property="SelectedValuePath" Value="Type"/>
                                        <Setter Property="IsEditable" Value="False"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridComboBoxColumn.ElementStyle>
                                <DataGridComboBoxColumn.EditingElementStyle>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="ItemsSource" Value="{Binding DataContext.VarTypes, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                        <Setter Property="DisplayMemberPath" Value="Name"/>
                                        <Setter Property="SelectedValuePath" Value="Type"/>
                                        <Setter Property="IsEditable" Value="False"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridComboBoxColumn.EditingElementStyle>
                            </DataGridComboBoxColumn>
                            <DataGridTextColumn Header="{x:Static helper:ProjectEditorWindowText.VariableValue}" Binding="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="250" MinWidth="150"/>
                            <DataGridTextColumn Header="{x:Static helper:ProjectEditorWindowText.VariableDescription}" Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*" MinWidth="200"/>
                            <DataGridTemplateColumn Header="{x:Static helper:ProjectEditorWindowText.HeaderActions}" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="{x:Static helper:ProjectEditorWindowText.IconClone}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCloneVariable}" Click="CloneVariable_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Width="30" Margin="2" Padding="2"/>
                                            <Button Content="{x:Static helper:ProjectEditorWindowText.IconDelete}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipRemoveVariable}" Click="RemoveVariable_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Width="30" Margin="2" Padding="2" Foreground="Red"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <avalonedit:TextEditor x:Name="txtVariablesSource" Grid.Row="1" FontFamily="Consolas" FontSize="12" ShowLineNumbers="True" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                        <Button Content="{x:Static helper:ProjectEditorWindowText.AddVariable}" Click="AddVariable_Click" Style="{StaticResource ActionButton}" Background="#FF4CAF50" Foreground="White" Margin="0,0,10,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.GenerateCode}" Click="GenerateVariablesCode_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static helper:ProjectEditorWindowText.GenerateCodeNote}" VerticalAlignment="Center" Foreground="Gray" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="{x:Static helper:ProjectEditorWindowText.TabModEvent}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <GroupBox Grid.Column="0" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.ModEventFiles}" Margin="0,0,10,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconFolder}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCreateEventFolder}" Click="CreateModEventFolder_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconDelete}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipDeleteEventFolder}" Click="DeleteModEventFolder_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding SelectedEventItem.IsFolder, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconAdd}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCreateModEvent}" Click="CreateModEvent_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconClone}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipCloneModEvent}" Click="CloneModEvent_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconEdit}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipRenameModEvent}" Click="RenameModEvent_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" Margin="0,0,2,0" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.IconRemove}" ToolTip="{x:Static helper:ProjectEditorWindowText.TooltipDeleteModEvent}" Click="DeleteModEvent_Click" Style="{StaticResource ActionButton}" Width="30" Height="30" FontSize="14" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}"/>
                            </StackPanel>
                            <TreeView Grid.Row="1" Name="tvModEventFiles" ItemsSource="{Binding EventItems, Mode=OneWay}" SelectedItemChanged="TreeView_EventSelectedItemChanged">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsExpanded" Value="True"/>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding IsFolder, Converter={StaticResource BoolToIconConverter}}" FontFamily="Segoe UI Symbol" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding Name}" Padding="2"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </GroupBox>
                    <GroupBox Grid.Column="1" Grid.Row="0" Header="{x:Static helper:ProjectEditorWindowText.ModEventEditor}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                <Button x:Name="btnGuiMode" Content="{x:Static helper:ProjectEditorWindowText.GuiMode}" Click="ToggleGuiMode_Click" Style="{StaticResource ActionButton}" Background="#FF2E5090" Foreground="White" Width="120" Margin="0,0,5,0"/>
                                <Button x:Name="btnCodeMode" Content="{x:Static helper:ProjectEditorWindowText.CodeMode}" Click="ToggleCodeMode_Click" Style="{StaticResource ActionButton}" Width="120" Margin="0,0,20,0"/>
                                <Button Content="{x:Static helper:ProjectEditorWindowText.SaveModEvent}" Click="SaveModEvent_Click" Style="{StaticResource PrimaryButton}" Width="100" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}"/>
                            </StackPanel>
                            <Grid x:Name="guiModePanel" Grid.Row="1" Visibility="Visible">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <Grid Margin="10" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="10"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="10"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="10"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <GroupBox Header="{x:Static helper:ProjectEditorWindowText.BasicSettings}" Grid.Row="0">
                                            <StackPanel>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.Mode}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                    <ComboBox Grid.Column="1" x:Name="cmbEventMode" ItemsSource="{Binding EventModeOptions}" SelectedItem="{Binding SelectedModEvent.EventMode, Mode=TwoWay}" Height="28" SelectionChanged="EventMode_SelectionChanged"/>
                                                    <TextBlock Grid.Column="2" x:Name="txtOrderIndexLabel" Text="{x:Static helper:ProjectEditorWindowText.OrderIndex}" VerticalAlignment="Center" Margin="10,0,10,0"/>
                                                    <TextBox Grid.Column="3" x:Name="txtOrderIndex" Text="{Binding SelectedModEvent.OrderIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" PreviewTextInput="NumberOnly_PreviewTextInput"/>
                                                    <TextBlock Grid.Column="4" x:Name="txtCacheTypeLabel" Text="{x:Static helper:ProjectEditorWindowText.CacheType}" VerticalAlignment="Center" Margin="10,0,10,0"/>
                                                    <ComboBox Grid.Column="5" x:Name="cmbCacheType" ItemsSource="{Binding CacheTypes}" SelectedItem="{Binding SelectedModEvent.CacheType, Mode=TwoWay}" Height="28"/>
                                                    <TextBlock Grid.Column="6" x:Name="txtWorkOnLabel" Text="{x:Static helper:ProjectEditorWindowText.WorkOn}" VerticalAlignment="Center" Margin="10,0,10,0"/>
                                                    <ComboBox Grid.Column="7" x:Name="cmbWorkOn" ItemsSource="{Binding WorkOnTypes}" SelectedItem="{Binding SelectedModEvent.WorkOn, Mode=TwoWay}" Height="28"/>
                                                </Grid>
                                                <Grid x:Name="gridCustomEventName" Margin="0,0,0,0" Visibility="Collapsed">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.EventMethodName}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                    <TextBox Grid.Column="1" Text="{Binding SelectedModEvent.CustomEventName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" ToolTip="{x:Static helper:ProjectEditorWindowText.EventMethodNameTooltip}"/>
                                                </Grid>
                                            </StackPanel>
                                        </GroupBox>
                                        <GroupBox x:Name="grpEventSelection" Header="{x:Static helper:ProjectEditorWindowText.Event}" Grid.Row="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Button Grid.Column="0" Content="{x:Static helper:ProjectEditorWindowText.AddEvent}" Click="AddEvent_Click" Style="{StaticResource ActionButton}" Background="#FF4CAF50" Foreground="White" Height="24" Padding="10,0" Margin="0,0,10,0" HorizontalAlignment="Left"/>
                                                <ComboBox Grid.Column="1" x:Name="cmbEvents" Height="28" DisplayMemberPath="DisplayName" SelectedValuePath="Name" SelectedValue="{Binding SelectedModEvent.SelectedEvent, Mode=TwoWay}"/>
                                            </Grid>
                                        </GroupBox>
                                        <GroupBox Header="{x:Static helper:ProjectEditorWindowText.Conditions}" Grid.Row="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Button VerticalAlignment="Top" Grid.Column="0" Content="{x:Static helper:ProjectEditorWindowText.AddCondition}" Click="AddCondition_Click" Style="{StaticResource ActionButton}" Background="#FF4CAF50" Foreground="White" Height="24" Margin="0,0,10,0"/>
                                                <ListBox x:Name="lstConditions" Grid.Column="1" ItemsSource="{Binding SelectedModEvent.Conditions}" AllowDrop="True" Drop="Conditions_Drop" PreviewMouseLeftButtonDown="Conditions_PreviewMouseLeftButtonDown" Height="80">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding Code}" TextWrapping="Wrap" FontSize="11"/>
                                                                <Button Grid.Column="1" Content="{x:Static helper:ProjectEditorWindowText.IconRemove}" Click="RemoveCondition_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Width="24" Height="24" Padding="0"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                            </Grid>
                                        </GroupBox>
                                        <GroupBox Header="{x:Static helper:ProjectEditorWindowText.Actions}" Grid.Row="6">
                                            <Grid Margin="10">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <Button Grid.Row="0" Content="{x:Static helper:ProjectEditorWindowText.AddAction}" Click="AddAction_Click" Style="{StaticResource ActionButton}" Background="#FF4CAF50" Foreground="White" Height="24" Padding="10,0" Margin="0,0,0,5" HorizontalAlignment="Left"/>
                                                <ListBox x:Name="lstActions" Grid.Row="1" ItemsSource="{Binding SelectedModEvent.Actions}" MinHeight="120" AllowDrop="True" Drop="Actions_Drop" PreviewMouseLeftButtonDown="Actions_PreviewMouseLeftButtonDown">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding Order}" FontWeight="Bold" FontSize="11" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="1" Text="{Binding Code}" TextWrapping="Wrap" FontSize="11"/>
                                                                <Button Grid.Column="2" Content="{x:Static helper:ProjectEditorWindowText.IconRemove}" Click="RemoveAction_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Width="24" Height="24" Padding="0"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                            </Grid>
                                        </GroupBox>
                                    </Grid>
                                </ScrollViewer>
                            </Grid>
                            <Grid x:Name="codeModePanel" Grid.Row="1" Visibility="Collapsed">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Background="#FFF5F5F5">
                                        <Button Content="{x:Static helper:ProjectEditorWindowText.SearchAndReplace}" Click="ReplaceInEventEditor_Click" Style="{StaticResource ActionButton}" Width="130" Height="28" Margin="0,0,5,0" Background="#FF4CAF50" Foreground="White" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}"/>
                                    </StackPanel>
                                    <Border x:Name="eventReplacePanel" Grid.Row="1" Background="#FFF9F9F9" BorderBrush="#FFCCCCCC" BorderThickness="0,1,0,1" Padding="10" Visibility="Collapsed">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.Find}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBox x:Name="txtEventFindText" Grid.Row="0" Grid.Column="1" Height="26" VerticalContentAlignment="Center" TextChanged="TxtEventFindText_TextChanged" PreviewKeyDown="TxtEventFindText_KeyDown" Margin="0,0,10,5"/>
                                            <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal">
                                                <Button Content="{x:Static helper:ProjectEditorWindowText.FindNext}" Click="EventFindNext_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Margin="0,0,5,5"/>
                                                <Button Content="{x:Static helper:ProjectEditorWindowText.Close}" Click="CloseEventReplacePanel_Click" Style="{StaticResource ActionButton}" Width="60" Height="26" Margin="0,0,0,5"/>
                                            </StackPanel>
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static helper:ProjectEditorWindowText.Replace}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBox x:Name="txtEventReplaceText" Grid.Row="1" Grid.Column="1" Height="26" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                                            <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal">
                                                <Button Content="{x:Static helper:ProjectEditorWindowText.ReplaceButton}" Click="EventReplaceOne_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Margin="0,0,5,0"/>
                                                <Button Content="{x:Static helper:ProjectEditorWindowText.ReplaceAll}" Click="EventReplaceAll_Click" Style="{StaticResource ActionButton}" Width="90" Height="26" Background="#FF2196F3" Foreground="White"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <avalonedit:TextEditor x:Name="txtEventSourceEditor" Grid.Row="2" FontFamily="Consolas" FontSize="12" ShowLineNumbers="True" IsEnabled="{Binding HasSelectedEventFile, Mode=OneWay}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </GroupBox>
                    <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{x:Static helper:ProjectEditorWindowText.OpenInExplorer}" Click="OpenModEventFolder_Click" Style="{StaticResource ActionButton}" Width="150" Margin="0,0,5,0"/>
                        <Button Content="{x:Static helper:ProjectEditorWindowText.Refresh}" Click="RefreshTab_Click" Style="{StaticResource ActionButton}" Width="100"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
        <Border Grid.Row="2" Background="#FFF0F0F0" Padding="10,5" BorderBrush="#FFDDDDDD" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding Project.ProjectPath, Mode=OneWay}" FontSize="11" Foreground="Gray"/>
        </Border>
    </Grid>
</local:CWindow>
