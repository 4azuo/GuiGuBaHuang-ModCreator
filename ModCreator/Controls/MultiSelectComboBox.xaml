<UserControl x:Class="ModCreator.Controls.MultiSelectComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="30" d:DesignWidth="150">
    <ComboBox x:Name="MainComboBox" BorderThickness="0" Padding="0" VerticalContentAlignment="Center" 
              Text="{Binding ElementName=Root, Path=DisplayText, Mode=OneWay}" IsEditable="True" IsReadOnly="True"
              DropDownOpened="MainComboBox_DropDownOpened">
        <ComboBox.ItemTemplate>
            <DataTemplate>
                <CheckBox Content="{Binding DisplayName}" 
                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                          Click="CheckBox_Click"/>
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>
